% Apply 3D median filter using GPU
%   out = ADDME(in, filter_size, padopt)
%      in          : Input 3D matrix
%      filter_size : Filter size
%      padopt      : Padding option ('mirror', 'wrap', 'clamp' or
%      'border')